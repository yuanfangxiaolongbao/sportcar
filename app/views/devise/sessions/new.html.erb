<!-- <h2>Log in</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, required: false, autofocus: true %>
    <%= f.input :password, required: false %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in" %>
  </div>
<% end %>

<%= render "devise/shared/links" %> -->

<div class="row user_setup">
  <div class="col-md-4 col-md-offset-4 col-xs-8 col-xs-offset-2 user_setup_content">
    <div class="col-md-10 col-md-offset-1 col-xs-10 col-xs-offset-1">
    <h2><sapn class="fa fa-sign-in"></sapn> 帐号登录</h2>
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email, required: false, autofocus: true %>
          <%= f.input :password, required: false, label:"密码" %>
          <%= f.input :remember_me,  label:"记住我",as: :boolean if devise_mapping.rememberable? %>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "登录",class: "btn btn-primary" %>
        </div>
      <% end %>
      <hr />
      <div class="account">
        <%= link_to "没有帐号，立即注册", new_registration_path(resource_name) %>
      </div>
    </div>
  </div>
</div>
